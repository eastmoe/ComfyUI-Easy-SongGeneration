
# Deps from ComfyUI.
# einops>=0.8.1
# pydantic>=2.0
# PyYAML>=6.0
# safetensors>=0.4.3
# torch>=2.6.0,<2.12
# torchaudio>=2.6.0,<2.12
# tqdm>=4.66.0
# transformers>=4.37.2,<6.0


# alias-free-torch>=0.0.6  # replaced by local alias_free_torch shim
# einops-exts>=0.0.4  # replaced by local einops_exts.py helpers backed by einops
# julius>=0.2.7  # replaced by torchaudio.functional.resample shim
# k-diffusion>=0.1.1,<0.2  # replaced by local k_diffusion.sampling inference subset; current Flow1dVAE inference uses its CFM Euler solver
# vector-quantize-pytorch>=1.22.17  # replaced by Flow1dVAE/libs/rvq/descript_quantize3.py local RVQ implementation

# diffusers>=0.37.1,<0.38  # replaced by local Flow1dVAE/models_gpt/models/diffusers_compat.py subset
# huggingface-hub>=0.34.0,<2.0 # required by transformers
# openunmix>=1.3.0 # optional: only needed by bundled Demucs non-CAC Wiener filtering
# lameenc>=1.8.1  # optional: only needed by bundled Demucs MP3 export
# librosa>=0.11.0  # replaced by torchaudio.load/resample plus a stdlib PCM WAV fallback
# omegaconf>=2.2.0,<3.0  # replaced by local omegaconf.py static config compatibility subset
# packaging>=24.0  # no direct runtime import in this project
# x-transformers>=2.3.25  # no direct runtime import; transformer/RMSNorm/rotary layers are local
